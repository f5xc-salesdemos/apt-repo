# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Node
node_modules/
dist/
*.tgz

# Chrome extension signing key (never commit)
key.pem

# Environment
.env
.env.*

# Logs
*.log

# Test/UAT artifacts (headless-render screenshots, scratch HTML, etc.)
.artifacts/

# Astro
.astro/
site/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Docker
.docker-data/

# Go
*.exe
*.exe~
*.dll
*.dylib
*.test
*.out
# Root-anchored: `go mod vendor` writes at the module root, whereas an unanchored
# `vendor/` also swallows deliberately-committed vendored trees such as a browser
# extension's src/vendor/. A repo with a NESTED Go module wanting its vendor tree
# ignored should add that path locally; no such module exists in the fleet today
# (the one Go repo has go.mod at its root and does not vendor). See #794.
/vendor/

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Python virtual environments
venv/
env/
ENV/
.venv/
# A directory-only pattern does not match a worktree's virtual-environment
# symlink, so protect both forms from accidental staging.
.venv

# Test coverage
.coverage
htmlcov/
coverage/*
# The MCN SMSv2 coverage harness is authored source, not generated coverage output.
!coverage/smsv2/
!coverage/smsv2/**
coverage/smsv2/**/*.tfstate
coverage/smsv2/**/*.tfstate.*

# Build artifacts
reports/

# Projects
projects/

# Tool-specific
.claude/settings.local.json
.claude/todos/
.claude/worktrees/
.claude/scheduled_tasks.lock
.remember/
.playwright-mcp/
.serena/
.auto-claude/
claudedocs/
cache/
src/generated/
out/

# Bun
*.bun-build

# OpenCode plugin
.sisyphus/*
!.sisyphus/rules/
packages/*/bin/
test-injection/
notepad.md
oauth-success.html
.omx/

# Lock file preferences (Bun-based repos)
yarn.lock
/bun.lock

# Terraform provider build artifacts
/terraform-provider-xcsh

# Monorepo build
.turbo/
ts-dist/
target/
.sst/
tsconfig.tsbuildinfo

# Nix
.direnv/
/result

# Workspace (includes git worktree dirs for AI agents and bare-repo workflows)
.worktrees/
worktrees/
.wt/
.codex/
playground/

# Locally cloned dev repos (checked out alongside the parent, not tracked by it)
starlight-mega-menu/

# Documentation build artifacts
docs/_data/

# API spec pipeline
specs/
docs/specifications/api/
docs/api-reference/
.version
.etag
.etag_cache
.github_release

# Autoresearch session artifacts
autoresearch.sh
autoresearch.md
autoresearch.program.md
autoresearch.checks.sh
autoresearch-loop.sh
autoresearch.ideas.md
autoresearch-queries.txt
autoresearch.jsonl
.autoresearch/

# Session-resume files
handoff.md

# Union from xcsh fork (promoted 2026-04-19)
.npm/
packages/*/dist/
packages/*/dist-chrome/
packages/*/dist-firefox/
*.tsbuildinfo
*.node
*.b64.js
.zed/
*.kate-swp
*.tmp
tui-debug.log
*.cpuprofile
.nyc_output/
packages/ai/test/.temp-images/
superpowers/
.superpowers/
!.xcsh/
.xcsh/plugins/
.xcsh/contexts/
.pi_config/
.opencode/
.worktree-test-baseline.txt
compaction-results/
changes/
syntax.jsonl
out.jsonl
out.html
pi-*.html
packages/coding-agent/src/internal-urls/docs-index.generated.ts
packages/coding-agent/src/internal-urls/build-info.generated.ts
packages/typescript-edit-benchmark/runs/

# Local review working artifacts. No trailing slash, so symlinks match too.
.codex-review
.agy-review.*
